<launch>

	<arg name="project" default="lidar_localization"/>

    <param name="robot_description" command="$(find xacro)/xacro $(find lidar_localization)/launch/config/robot.urdf --inorder" />

    <node name="robot_state_publisher" pkg="robot_state_publisher" type="robot_state_publisher" respawn="true">
    <!-- <param name="tf_prefix" value="$(env ROS_HOSTNAME)"/> -->
    </node>
  
</launch>